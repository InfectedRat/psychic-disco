function mySquare(x) {
    return x*x;
  }

  function factorial(x) {        
    if (x <= 1) return 1;        
    return x * factorial(x-1);   
  }

  function mySquare(x) {
    return x*x;
  }

  function factorial(x) {        
    if (x <= 1) return 1;        
    return x * factorial(x-1);   
  }